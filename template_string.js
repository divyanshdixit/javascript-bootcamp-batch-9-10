const age = 25;
const younger = "younger";
const elder = "elder";
const name = "Divyansh";
const city = "Kanpur";

"My name is " + name + " "

`My name is ${name}, i'm ${age} years younger or elder , i live in ${city}`

`${(age>25) ? `${younger}` : `${elder}` }`

`string ` == 'string'
`${true ? `${younger}` : `false`}`